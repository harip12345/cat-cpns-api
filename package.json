{
  "name": "cat-cpns-api",
  "version": "1.0.0",
  "description": "Backend API Bank Soal CAT CPNS menggunakan Groq AI — untuk deployment di Vercel",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "groq-sdk": "^0.5.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
